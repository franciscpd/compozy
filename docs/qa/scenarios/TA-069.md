---
id: TA-069
area: TA
title: Revisioned Loop config and annotations API
persona: Ada
journey: J-07
expected: GET /config and compozy loop config read nullable stored config, required effective_config, and config_revision without mutation; guarded CLI, HTTP/UDS, and native writes apply semantic patches only at expected_revision, preserve the revision on no-op, increment it on change, and expose safe stale conflicts, while annotations persist editor positions and all sidecars remain workspace-scoped.
entry_points: compozy loop config|configure; compozy__loop_configure; HTTP/UDS Loop config and annotations routes
qa_status: untested
bug_ids: BUG-20260729-loop-sidecar-lifecycle
fix_status: fixed
retest_status:
fix_commits: 351f3535
evidence:
last_report:
overlaps:
---

story: As an agent or editor I manage Loop runtime defaults and diagram annotations per workspace.

Added by Task 14 QA tracker impact; flag only, not retested.

QA impact 2026-07-14: config reads now distinguish a known Loop with no stored override (`config: null`) from a missing Loop and include the effective first three config layers. Planning update only; remains untested.

QA execution 2026-07-29: alternating HTTP and UDS writes preserved workspace isolation, but the
initial response omitted `config: null`; missing names accepted annotation state and config mutated
before returning 404; delete left both sidecars to reappear after same-name recreation. Root fix and
focused regressions are green. A fresh rebuilt-candidate rewalk then passed explicit null, alternating
HTTP/UDS writes, missing-name no-mutation, two-workspace isolation, delete cleanup, and same-name
recreation. The tracker remains fail/pending until the governed fix commit exists.

QA impact 2026-08-25: Loop config now has a read-only CLI snapshot and a shared revisioned write
contract across CLI, HTTP/UDS, and `compozy__loop_configure`. Prior July evidence predates these
surfaces, so status and evidence were reset to `untested`; the batch QA phase owns the governed walk.

Walk:

1. On a known Loop without an override, read through HTTP and UDS and run
   `compozy loop config --workspace <ref> --name <loop> -o json`; require explicit `config: null`,
   `config_revision: 0`, matching `effective_config`, and no stored-row side effect.
2. Write a config file with `compozy loop configure --expected-revision 0`; require revision `1`.
   Repeat the same semantic patch at revision `1`; require the revision to remain `1`. Apply one
   changed field at revision `1`; require revision `2` and preservation of omitted fields.
3. Replay the now-stale revision `1` through HTTP and UDS; require `409` with fixed `error`,
   `expected_revision: 1`, and `current_revision: 2`, with no mutation. Confirm a negative revision
   is `400` before mutation and OpenAPI declares `minimum: 0`.
4. Resolve the live `compozy__loop_configure` descriptor. Guard one native patch with the current
   revision, then reuse that token; require one committed change and one tool conflict. Confirm a
   negative native revision is rejected before the API service and config remains unchanged.
5. Repeat the snapshot/CAS sequence against same-name Loops in two workspaces; require independent
   configs and revision sequences. Rewalk annotation persistence and delete/recreate cleanup from
   the July scenario so the expanded contract does not regress existing sidecars.

Expected evidence: CLI JSON and native structured transcripts, paired HTTP/UDS request/response
bodies for revision 0, no-op, change, stale 409, and negative validation, plus before/after store
observations and two-workspace isolation snapshots.

src: .compozy/tasks/loops/task_14.md
